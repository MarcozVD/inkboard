// Dynamic board route: never prerendered (unknown ids at build time).
// Served via the SPA fallback (index.html) in the static Tauri build.
export const prerender = false;
export const ssr = false;
